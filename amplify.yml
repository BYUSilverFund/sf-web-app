version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        # # Write environment variables to a file that Next.js will read during build
        # - echo "NEXT_PUBLIC_LAMBDA_POSITIONS_ENDPOINT=$LAMBDA_POSITIONS_ENDPOINT" >> .env.production
        # - echo "NEXT_PUBLIC_FASTAPI_URL=$FASTAPI_URL" >> .env.production
        # - echo "NEXT_PUBLIC_COGNITO_USER_POOL_ID=$COGNITO_USER_POOL_ID" >> .env.production
        # - echo "NEXT_PUBLIC_COGNITO_APP_CLIENT_ID=$COGNITO_APP_CLIENT_ID" >> .env.production
        # - echo "NEXT_PUBLIC_COGNITO_AWS_REGION=$COGNITO_AWS_REGION" >> .env.production
        - npm run build
  artifacts:
    baseDirectory: .next/
    files:
      - "/*"
  cache:
    paths:
      - node_modules/**/*
